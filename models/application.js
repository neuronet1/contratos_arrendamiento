var Application = function (args) {
    var app = {};

    // obtiene la lista de contratos, esta puede ser filtrada
    // por el nombre del trabajador
    app.get_contratos = function (filters) {

    };

    return app;
};

module.exports = Application;
